#include <stdio.h>

int main() {
    int alpha, bravo, charlie;
    float delta, echo, foxtrot;

    printf("%d %d %d\n", alpha, bravo, charlie);
    printf("%f %f %f\n", delta, echo, foxtrot);

    return 0;
}
